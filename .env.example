# Base URL API relatif (same-origin proxy via Cloudflare Worker).
# Di production: /api/v1  (diproxy Worker ke backend)
# Di local dev: http://localhost:3000/api/v1  (vite.config.ts proxy)
VITE_API_URL=/api/v1